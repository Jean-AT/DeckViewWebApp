# Vite (desarrollo). El proxy de Vite reenvía /api → http://localhost:3000
VITE_API_URL=/api

# Docker Compose
# Puerto del frontend en el host
PORT=8080
# Backend accesible desde el contenedor (sin barra final)
BACKEND_URL=http://host.docker.internal:3000
